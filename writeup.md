# 결제 통로를 클라이언트가 고르게 두면 생기는 일
### Claude "Cassia" 사례로 보는 클라이언트 신뢰경계(trust boundary) 취약점

> 이 글에서 다루는 구체적 취약점은 **이미 패치**되었다. 목적은 "이 API를 이렇게 때리면 뚫린다"가 아니라, **왜 이런 부류의 취약점이 반복해서 생기는지**를 이해하고 방어하는 것이다. 실제 서비스를 대상으로 한 우회 코드는 담지 않는다.

---

## 0. 한 문장 요약

서버가 **"이 사용자가 어떤 결제 수단을 쓸 수 있는가"**라는 정책 판단을 클라이언트로 내려보냈고, 하필 그 정책이 고른 결제 수단 중 하나(SEPA 직접출금)가 **돈이 실제로 빠져나가기 전에 서비스를 먼저 열어주는** 구조였다. 이 두 가지가 겹치는 지점에서 "JSON 필드 하나 고치면 유료 개통"이 성립했다.

## 1. 배경: 결제 플로우는 왜 서버에 물어보나

claude.ai에서 구독 업그레이드를 누르면, 프론트엔드는 결제 폼을 바로 띄우지 않고 먼저 서버에 묻는다.

```
GET /api/organizations/{org_id}/subscription/checkout_capabilities
```

이 응답이 "이 사용자에게 어떤 결제 UI를 보여줄지"를 결정한다. 서버는 IP 지역, 계정 등록 지역, `Accept-Language` 같은 신호를 종합해 결제 플로우 식별자를 돌려준다.

```json
{ "checkout_flow": "stripe" }
```

- `stripe` → 신용카드 폼 (카드번호, 유효기간, CVV, 3DS)
- `cassia` → 유럽 SEPA 은행출금 폼 (IBAN 입력)

여기까지는 지극히 평범한 설계다. 지역마다 결제 수단이 다르니 서버가 알려주는 건 자연스럽다. **문제는 이 응답을 프론트엔드가 그대로 신뢰하고, 서버는 이후 단계에서 그 선택을 다시 검증하지 않았다는 점이다.**

## 2. 신뢰경계가 어디서 깨졌나

보안에서 신뢰경계(trust boundary)는 "여기서부터는 못 믿는 데이터"라는 선이다. HTTP 응답이 브라우저에 도착하는 순간, 그 응답은 **더 이상 서버의 통제 아래 있지 않다.** 사용자는 브라우저 확장, 개발자도구, 프록시로 응답 본문을 얼마든지 바꿀 수 있다.

정상 흐름은 이렇다.

```
[서버] 정책 판단 → checkout_flow=stripe → [브라우저] 카드 폼 렌더 → 카드 제출 → [서버] 동기 승인
```

그런데 프론트엔드가 받은 `checkout_flow` 값을 그대로 믿고 렌더링을 분기한다면, 공격자는 브라우저 안에서 응답을 이렇게 바꿀 수 있다.

```json
{ "checkout_flow": "cassia" }
```

그러면 카드 폼 대신 IBAN 입력 폼이 뜬다. 즉 **결제 수단 선택권이 사실상 클라이언트에게 넘어간 것**이다. 서버가 "너는 카드로 결제해야 해"라고 정했더라도, 프론트가 그 값을 신뢰하는 이상 클라이언트가 "아니, 나는 은행출금으로 할래"로 덮어쓸 수 있다.

> 핵심: 취약점은 "응답을 조작할 수 있다"가 아니다. 응답 조작은 브라우저에서 **항상 가능하다.** 진짜 취약점은 **서버가 그 조작된 선택을 결제 제출 단계에서 다시 검증하지 않은 것**이다.

## 3. 왜 하필 은행출금(SEPA)이 문제였나

결제 수단을 바꿀 수 있다는 것만으로는 "공짜 구독"이 되지 않는다. 카드로 바꿔봐야 유효한 카드가 없으면 승인이 안 난다. 이 공격이 성립한 이유는 **카드와 SEPA 직접출금의 신뢰 모델이 근본적으로 다르기 때문**이다.

| | 신용카드 (동기) | SEPA 직접출금 (비동기) |
| --- | --- | --- |
| 승인 방식 | 제출 즉시 은행이 온라인 승인 | 제출 후 배치 청산 (T+1~3영업일) |
| 제출 시점 검증 | 카드번호 Luhn + CVV + 3DS + 잔액/승인 | **IBAN 형식(MOD 97-10) 검증뿐** |
| 자금 확인 시점 | 밀리초 단위 | 며칠 뒤 청산 큐에서 |
| 실패 통보 | 즉시 거절 | 며칠 뒤 R-코드 반송 (R02 등) |

SEPA 직접출금은 제출 순간에 **"이 계좌가 실제로 존재하는가, 잔액이 있는가"를 확인하지 않는다.** IBAN이 형식적으로 올바른지(체크섬)만 본 뒤 청산 큐에 넣는다. 실제 검증은 며칠 뒤 은행 간 청산에서 일어난다.

### 확인 공백(confirmation gap)

```
IBAN 제출 → 형식 검증 통과 → 게이트웨이 "성공" 반환 → 서비스 개통
   ↓ (T+1 ~ T+3 영업일)
청산 시스템에서 실제 계좌 조회 → 없는 계좌 → R-코드로 반송 → 뒤늦게 구독 취소
```

이 **며칠간의 공백**이 취약점의 두 번째 축이다. "돈이 실제로 빠져나갔는지"를 확인하기 전에 서비스를 먼저 열어준 것. 형식만 맞는 (실존하지 않는) 계좌번호로도 이 공백 동안 유료 기능이 열린다.

### MOD 97-10은 무엇을 검증하고 무엇을 못 하나

IBAN 체크섬(ISO 7064 MOD 97-10)은 **형식 오류**만 잡는다 — 자릿수 실수, 오타. **현실 세계의 사실**(계좌 존재 여부, 잔액, 명의)은 전혀 검증하지 못한다. 형식적으로 유효하지만 실존하지 않는 IBAN을 만드는 건 순수 수학 문제라, 형식 검증만으로는 가짜 계좌를 걸러낼 수 없다.

## 4. 두 결함의 곱셈

두 결함을 따로 보면 각각은 치명적이지 않다.

- **결함 A (신뢰경계)**: 결제 수단 선택을 클라이언트가 덮어쓸 수 있다.
- **결함 B (확인 공백)**: 비동기 결제는 자금 확인 전에 서비스를 연다.

A만 있으면? 은행출금 폼으로 바꿔도, 서버가 청산 확인 후 개통했다면 공짜가 안 된다.
B만 있으면? 서버가 결제 수단을 강제했다면, 애초에 은행출금 폼에 도달할 수 없다.

**A × B**가 만나는 지점에서 "형식만 맞는 계좌번호 → 즉시 개통 → 며칠 뒤 취소"라는 완결된 사슬이 생긴다. 이게 이 사례의 본질이다.

## 5. 이건 하나의 버그가 아니라 하나의 클래스다

원 분석 글이 잘 짚은 부분이 이거다. 같은 골격의 취약점이 여러 플랫폼에서 반복된다.

| 사례 | 조작 대상 | 깨진 신뢰경계 |
| --- | --- | --- |
| 본 사례 | `checkout_capabilities` 응답 | 프론트가 결제 통로 결정을 신뢰 |
| 모바일 구독 토큰 주입 | 인앱결제 offer 토큰 | 서버가 토큰-계정 자격 결합을 미검증 |
| 영수증 재사용 | 스토어 영수증 | 서버가 영수증-제출자 계정 대응을 미검증 |
| 결제 확정 경쟁 조건 | 동시 요청의 상태 창 | 권한 발급과 결제 확정 사이의 시간차 |

공통 패턴을 추상화하면 이렇다.

> **클라이언트가 조작 가능한 상태**가 **서버의 가치 있는 결정**(무엇을 열어줄지, 얼마를 받을지)을 좌우하고,
> 그 결정과 **실제 대가 확인** 사이에 **시간·검증 공백**이 있으면,
> 공백 동안 대가 없이 가치를 얻을 수 있다.

이 패턴을 가진 대상은 다음 세 조건을 동시에 만족한다.

1. 결제/기능 결정이 클라이언트 통제 상태에 의존한다.
2. 비동기로 확인되는 경로가 최소 하나 존재한다.
3. "서비스 개통"이 "대가 확정"보다 먼저 일어난다.

## 6. 방어 — 설계로 닫는 법

구체적 엔드포인트를 패치하는 것보다, **클래스 자체를 닫는** 설계 원칙이 중요하다.

1. **결제 통로 결정은 서버에서 닫아라.** `checkout_flow` 같은 값은 서버가 지역·계정·리스크 신호로 독립 계산하고, 프론트는 렌더러일 뿐이어야 한다. **제출 단계에서 다시** "이 사용자가 이 통로를 쓸 자격이 있는가"를 검증해야 한다. 응답을 내려보내는 것 자체는 괜찮다 — 그 값을 되돌아온 제출에서 재검증하지 않는 것이 문제다.
2. **비동기 결제는 개통을 지연하라.** SEPA·은행출금처럼 나중에 청산되는 수단은, 청산 성공 통보 전까지 유료 기능을 열지 말 것. "결제 처리 중" 상태를 두고 확인 후 활성화한다.
3. **자격과 자금 확인을 분리하되 순서를 지켜라.** "돈을 받았다"가 확정되기 전에는 "권한을 줬다"가 되지 않도록, 개통을 결제 확정 이벤트에 묶는다(웹훅/청산 콜백 기준).
4. **중요 응답에 서명(HMAC)을.** 근본 해결은 아니지만(공격자가 검증 로직도 후킹 가능), 공격 난도를 "필드 하나 수정"에서 "서명 구현 역공학"으로 올린다.
5. **계좌 실시간 검증.** 가능하면 제출 시점에 IBAN 실재성 검증 서비스를 연동해 형식 검증 이상을 수행한다.

## 7. 탐지 관점

방어자가 볼 수 있는 신호들.

- 동일 org/계정에서 "정책은 카드로 내렸는데 제출은 은행출금"인 불일치가 반복된다.
- "개통 → 며칠 뒤 청산 반송(R-코드) → 취소" 사이클이 특정 계정군에서 높은 비율로 발생한다.
- `checkout_capabilities` 응답과 실제 제출된 `checkout_flow`의 불일치율이 특정 IP/디바이스 지문에서 튄다.

가치 있는 "신호 엔드포인트"는 결제 로직에 영향을 주면서 클라이언트가 값을 볼 수 있는 것들이다: `checkout_capabilities`, `payment_methods`, `available_plans`, `eligible_promotions`, `feature_flags`, `entitlements` 등. 이런 엔드포인트는 "응답을 신뢰하는가"를 항상 의심해야 한다.

## 8. 맺음

이 사례의 교훈은 코드가 복잡해서가 아니라 **경계를 잘못 그었기 때문에** 뚫렸다는 것이다. "프론트엔드와 백엔드 사이의 보이지 않는 신뢰선"을 어디에 긋느냐가 전부다. 결제처럼 돈이 걸린 결정은 **클라이언트가 관측만 하고, 결정과 확정은 서버가 닫아야** 한다.

도구는 패치되지만, 패턴은 기억할 가치가 있다.

---

### 참고
- 원 분석: [焚决 Claude — caowo.de](https://blog.caowo.de/posts/claude-cassia-mock-payment-bypass-analysis/)
- SEPA Direct Debit / SEPA CORE 규칙 (EPC)
- ISO 7064 MOD 97-10 (IBAN 체크섬)
